type Query {
    feed(token: String!, limit: Int = 10, cursor: String): FeedPage!
}

type FeedPage {
    items: [FeedItem!]!
    nextCursor: String
    hasMore: Boolean!
}

type FeedItem {
    postId: Int!
    authorId: String!
    content: String!
    timestamp: String!
}
